--[[
    DorpOS :: phone/system/theme/themes/light.lua
    Light theme.
]]
return {
    name        = "Light",
    id          = "light",

    bg          = colors.white,
    bgCard      = colors.lightGray,
    bgInput     = colors.lightGray,
    bgStatus    = colors.lightGray,
    bgDock      = colors.lightGray,
    bgOverlay   = colors.white,

    text        = colors.black,
    textMuted   = colors.gray,
    textOnAccent= colors.white,

    accent      = colors.blue,
    accentDark  = colors.cyan,

    success     = colors.green,
    warning     = colors.yellow,
    danger      = colors.red,
    info        = colors.blue,

    border      = colors.gray,

    statusBarText   = colors.black,
    statusBarBg     = colors.lightGray,
    dockBg          = colors.lightGray,
    dockText        = colors.black,
    iconBg          = colors.white,
    iconText        = colors.black,
    notifBg         = colors.white,
    notifText       = colors.black,
    keyboardBg      = colors.lightGray,
    keyboardKey     = colors.white,
    keyboardText    = colors.black,
    keyboardSpecial = colors.blue,
}
